# Web Framework
flask>=3.0.0

# Deep Learning Framework
# Note: For production, we use the standard CPU version of torch
# to keep the deployment image size smaller and compatible with Render.
torch>=2.0.0

# Data Visualization (Required by your train.py)
matplotlib>=3.7.0

# Production WSGI Server
gunicorn>=21.0.0
